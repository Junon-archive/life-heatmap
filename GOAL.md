# Goal: Life Heatmap 웹앱 구현

`/home/junon/life_heatmap`에서 작업한다. 명세서는 이미 완성되어 있다. 너의 임무는 명세서를 읽고 v1을 끝까지 구현·검증하는 것이다.

## 최우선 규칙
1. 시작 전에 `CLAUDE.md`와 `specs/00_overview.md`~`specs/04_architecture.md` 다섯 문서를 **전부 원문으로** 읽어라. 이 문서들이 유일한 명세(SSOT)다. 이 프롬프트는 요약일 뿐이며, 충돌 시 specs가 우선한다. 요구사항(R-##)과 결정 로그(D-#)를 임의로 바꾸지 마라.
2. **어휘 규칙(절대)**: 코드·주석·문서·커밋 메시지·UI 문구 어디에도 개인 기록의 민감한 용도를 유추할 수 있는 단어를 쓰지 마라. 예시 히트맵은 "기상/출근", "퇴근/취침", "운동"이라는 이름으로만 지칭한다.
3. 색/기호/숫자의 "의미"를 코드나 UI 문구에 하드코딩하지 마라. 앱은 표기 도구만 제공한다.
4. 의존성은 specs/04 §1 목록(Vite, Preact, TypeScript, vite-plugin-pwa, vitest)만. 차트·날짜 라이브러리 도입 금지.
5. 구현 중 명세와 다르게 해야 할 사정이 생기면, 먼저 specs를 수정하고 `specs/01_requirements.md` 결정 로그에 근거를 남긴 뒤 구현하라.

## 구현 범위 (전부 v1)
`web_app/`에 specs/04 §2 디렉터리 구조대로:
- 일반화 히트맵 CRUD(추가/이름 변경/순서 변경/보관/삭제) + 3유형(basic / streak / conditional).
- 칸 탭 → 바텀시트(모바일)/팝오버(데스크톱) 즉시 편집: 채우기(없음/빗금/단색 × 6색), 마크(숫자 0~99 또는 ○/X/★, 색 선택), 테두리 강조 토글, streak 실패 체크, conditional 수치 입력, 지우기. 저장 버튼 없이 즉시 저장, 편집기 연 채로 다른 칸 탭 시 대상만 전환.
- 일요일 시작 7칸 주 그리드: 최근 12주 기본 + 위로 스크롤 시 과거 로드 + 연도 점프. 초기 스크롤은 오늘 행이 보이는 영역 하단 1/4 지점(최상단 금지). 행 라벨 토글(날짜 범위 ↔ W#). 지난 빈칸=회색 점, 오늘=아웃라인, 미래=40% 투명(입력 가능, 잠금 금지).
- streak: 마지막 실패 기준 연속일 계산, 5단계 그라데이션, auto/manual 채색 모드, 사용자 지정 마일스톤 테두리 이펙트, 현재/최고 연속일 표시.
- conditional: 사용자 정의 구간 규칙 자동 채색, 수치 표시 토글.
- 통계: 카드 하단 한 줄 요약 + 월/분기 집계 오버레이(CSS 바만 사용).
- 범례(기본 숨김), JSON 백업 내보내기/가져오기(병합/전체 교체), 설정 뷰.
- 동기화: `functions/api/data.ts`(GET/PUT, KV 바인딩 `LH_KV`), SHA-256(동기화 코드) 키, 엔트리 단위 LWW 병합 + tombstone, 3초 디바운스 push, 탭 복귀 시 pull, 오프라인 큐, 헤더 동기화 인디케이터. 동기화 미설정 시에도 로컬 전용으로 완전 동작.
- PWA: manifest, 아이콘, 오프라인 캐시.
- 모바일 우선(375px 기준), 히트맵 좌우 스와이프 전환, 데스크톱은 나란히 그리드.

세부 스키마·계산 규칙·병합 규칙은 specs/02, 화면·팔레트·상태별 렌더링은 specs/03을 원문 그대로 따르라.

## 작업 순서
1. 스캐폴드 + 데이터 모델/store/localStorage 영속화 + `dates`/`streak`/`merge`/`stats` 순수 유틸 및 vitest 테스트(specs/04 §5 케이스 필수: 연 경계 주 번호, 실패·미입력 혼재 streak, LWW·tombstone).
2. 그리드 렌더 + 칸 상태 + 칸 편집기.
3. 히트맵 관리/설정 뷰 + 유형별 로직.
4. 통계·범례·백업·스와이프·연도 점프.
5. 동기화(Functions+KV). 로컬 검증은 `wrangler pages dev` 사용.
6. PWA + 모바일 폴리시 마감.

## 검증·완료 기준
- `npm run build` 성공, vitest 전부 통과.
- dev 서버 실행 후 핵심 플로우 실제 확인: 히트맵 3개 생성 → 유형별 칸 입력 → 새로고침 후 데이터 유지 → 백업 내보내기/가져오기 → 375px 뷰포트 확인.
- git init(이미 있으면 생략) 후 단계별 의미 있는 커밋. `temp/`는 .gitignore에 추가.
- 각 단계 완료 시 `Roadmap.md` 갱신(없으면 생성).
- 완료 보고에 사용자가 직접 수행할 Cloudflare 배포 체크리스트(specs/04 §4)를 포함하라.
